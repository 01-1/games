{
	# Optional global Caddy settings belong here.
}

{$GAMES_SITE:localhost} {
	import games.routes.caddy
	# The imported routes serve the landing page and games; everything else is unknown.
	respond 404
}
